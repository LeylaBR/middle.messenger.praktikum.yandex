export const template = `<input {{required}} class="{{className}}" id="{{ name }}" type="{{ type }}" value="{{ value }}" placeholder="{{ placeholder }}" name="{{ name }}"/>
<span class='errorField' id="error_{{name}}" class="hide"></span>`;
